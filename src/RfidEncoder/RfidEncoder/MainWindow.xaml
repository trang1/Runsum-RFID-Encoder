<Window x:Class="RfidEncoder.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:viewModels="clr-namespace:RfidEncoder.ViewModels"
        Title="Runsum RFID Encoder v0.1" Height="350" Width="525"
        MinWidth="500" MinHeight="600"
        DataContext="{x:Static viewModels:MainWindowViewModel.Instance}"
        SizeToContent="WidthAndHeight"
        Icon="Resources/rfid.ico"
        Background="DarkGray">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        
        <GroupBox Header="Connection">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Text="Port" Margin="3"/>
                <ComboBox Grid.Column="1" ItemsSource="{Binding ComPorts}" 
                          SelectedItem="{Binding SelectedComPort}" Margin="3" />

                <TextBlock Text="Region" Margin="3" Grid.Row="1"/>
                <ComboBox Grid.Column="1" Grid.Row="1" ItemsSource="{Binding Regions}" IsEnabled="{Binding IsConnected}"
                          SelectedItem="{Binding SelectedRegion}" Margin="3" />

                <TextBlock Text="Baud rate" Margin="3" Grid.Row="2"/>
                <ComboBox Grid.Column="1" Grid.Row="2" ItemsSource="{Binding BaudRates}" DisplayMemberPath="Title" SelectedValuePath="Id"
                          SelectedItem="{Binding SelectedBaudRate}" Margin="3" />

                <Button Grid.Row="3" Content="Connect" Command="{Binding ConnectCommand}" Margin="3"/>
            </Grid>    
        </GroupBox>

        <GroupBox Header="Race" Grid.Row="1">
            
        </GroupBox>

        <GroupBox Header="Encoding" Grid.Row="2">
            <TabControl>
                <TabItem Header="List">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        
                        <StackPanel Orientation="Horizontal" >
                            <Button Content="Start encoding" />
                        </StackPanel>

                        <DataGrid Grid.Row="1" Margin="3">
                            <DataGrid.Columns>
                                <DataGridTextColumn Header="Race #" />
                            </DataGrid.Columns>
                        </DataGrid>
                    </Grid>
                </TabItem>
                <TabItem Header="Single">
                    
                </TabItem>
            </TabControl>
        </GroupBox>
    </Grid>
</Window>
